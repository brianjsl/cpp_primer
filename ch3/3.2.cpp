#include <iostream>
#include <string>
using std::cin;
using std::string;

int main() {
  string input;
  // Read string line at a time
  // while (getline(cin, input)) {
  // };

  while (cin >> input) {
    // Read a word at a time
  };
  return 0;
}