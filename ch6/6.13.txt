voidf(T) takes the value with type T and copies it as a parameter while
T& takes a reference so the actual argument passed in can also be changed
while in the regular T case the argument itself cannot be changed.